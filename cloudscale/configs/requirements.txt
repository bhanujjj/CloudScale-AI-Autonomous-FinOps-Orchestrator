# CloudScale AI Orchestrator — Phase 1: RL Simulator
# Pinned for reproducibility on Colab T4.
#
# IMPORTANT for Colab: do NOT install torch or numpy here.
#   Colab pre-installs torch (with CUDA) and a NumPy build that is
#   binary-compatible with its pre-installed compiled packages. Changing
#   NumPy inside a live notebook runtime can trigger errors like:
#   "numpy.dtype size changed, may indicate binary incompatibility".
#   For local install: uncomment the bottom block.

# Core RL stack (leave torch + numpy to Colab)
gymnasium==0.29.1
stable-baselines3==2.3.2
pandas>=2.2,<3

# Hyperparameter search
optuna==3.6.1

# MLOps / experiment tracking
mlflow==3.13.0
dagshub==0.7.0

# Utilities
pyyaml>=6.0.1,<7
python-dotenv==1.0.1
matplotlib>=3.9,<4
tqdm>=4.67,<5
rich>=13.7,<14
structlog==24.2.0
setuptools>=60

# Testing
pytest==8.2.2
pytest-cov==5.0.0

# ---------- LOCAL INSTALL (uncomment if running on your Mac, NOT Colab) ----------
# On your Mac, torch + numpy are not pre-installed. Use this set:
# torch==2.3.1
# numpy==1.26.4
