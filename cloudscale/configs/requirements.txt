# CloudScale AI Orchestrator — Phase 1: RL Simulator
# Pinned for reproducibility on Colab T4.
#
# IMPORTANT: Do NOT install torch or numpy here.
#   Colab pre-installs torch (with CUDA) and numpy compiled against
#   Colab's pre-installed JAX/TF. Pinning different versions breaks
#   the binary ABI. Just use whatever Colab already has — it works.
#   For local install: uncomment the bottom block.

# Core RL stack (pure Python deps only)
gymnasium==0.29.1
stable-baselines3==2.3.2
# torch is pre-installed on Colab, do not pin
# numpy is pre-installed on Colab, do not pin
pandas==2.2.2

# Hyperparameter search
optuna==3.6.1

# MLOps / experiment tracking
mlflow==2.14.3
dagshub==0.3.14

# Utilities
pyyaml==6.0.1
python-dotenv==1.0.1
matplotlib==3.9.0
tqdm==4.66.4
rich>=13.1,<14
structlog==24.2.0
setuptools>=60,<80  # mlflow needs pkg_resources (removed in setuptools 80+)

# Testing
pytest==8.2.2
pytest-cov==5.0.0

# ---------- LOCAL INSTALL (uncomment if running on your Mac, NOT Colab) ----------
# On your Mac, torch + numpy are not pre-installed. Use this set:
# torch==2.3.1
# numpy==1.26.4
