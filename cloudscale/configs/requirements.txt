# CloudScale AI Orchestrator — Phase 1: RL Simulator
# Pinned for reproducibility. Colab will install these into its own runtime.
# Local install: pip install -r cloudscale/configs/requirements.txt

# Core RL stack
gymnasium==0.29.1
stable-baselines3==2.3.2
# torch + numpy: DO NOT install — Colab has pre-installed CUDA torch + numpy
#               compiled against Colab's pre-installed packages (JAX, TF, etc.)
#               Adding a different torch/numpy breaks the binary ABI.
#               All gymnasium + sb3 code works fine with Colab's versions.
# Local install (non-Colab): uncomment the next two lines
# torch==2.3.1
# numpy==1.26.4
pandas==2.2.2

# Hyperparameter search
optuna==3.6.1

# MLOps / experiment tracking
mlflow==2.14.3
dagshub==0.3.14

# Utilities
pyyaml==6.0.1
python-dotenv==1.0.1
matplotlib==3.9.0
tqdm==4.66.4
rich>=13.1,<14
structlog==24.2.0
setuptools>=60  # Colab has its own setuptools; no pin needed

# Testing
pytest==8.2.2
pytest-cov==5.0.0
